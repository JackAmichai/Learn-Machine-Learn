## 2024-05-18 - Missing ARIA Labels on Modals
**Learning:** Custom modal close buttons using a single `×` character frequently lack `aria-label` attributes, which makes them inaccessible to screen readers.
**Action:** Always add an `aria-label` to icon-only or single-character buttons in modals.
